import { Alert } from '@mui/material';
import React from 'react';

const TwoApp = () => {
    return (
        <div>
            <Alert color='success'>TwoApp</Alert>
        </div>
    );
};

export default TwoApp;